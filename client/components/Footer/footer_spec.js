import { expect } from 'chai'
import Footer from './index'
import styles from './style.css'

describe('Footer', () => {
  it('should exist', () => {
    expect(1).to.equal(1)
    expect(styles.normal).to.equal('.normal')
  })
})
